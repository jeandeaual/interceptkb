{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build (debug)",
            "type": "shell",
            "command": "mkdir -p build && cd build && cmake -DCMAKE_BUILD_TYPE=\"Debug\" .. && cmake --build .",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
